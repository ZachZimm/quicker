QUICKER_DATA_DIR=./data
QUICKER_COOKIE_SECURE=false
# Optional private property profile; defaults to QUICKER_DATA_DIR/private-profile.json.
# QUICKER_PROFILE_PATH=/path/to/private-profile.json
# Set true behind HTTPS. The model host and credentials are set in the authenticated Settings page.
